---
import Layout from '../layouts/Layout.astro';
import type { Tool } from '../types/tool';

// 加载工具数据
const toolsData = await import('../../public/tools.json');
const tools: Tool[] = toolsData.default as Tool[];

// 获取所有分类并统计
const categories = tools.reduce((acc, tool) => {
  const cat = tool.category || '未分类';
  acc[cat] = (acc[cat] || 0) + 1;
  return acc;
}, {} as Record<string, number>);

const categoryList = Object.entries(categories).sort((a, b) => b[1] - a[1]);
---

<Layout title="Batool - Tool Index">
  <!-- DaisyUI 主题容器 -->
  <div class="min-h-screen bg-base-100" data-theme="emerald">

    <!-- 顶部导航栏 - DaisyUI navbar -->
    <div class="navbar bg-base-100 shadow-sm px-6">
      <div class="flex-1">
        <a class="btn btn-ghost text-xl">Batool</a>
        <span class="text-sm text-base-content/60 ml-2 hidden sm:inline">Personal Tool Index</span>
      </div>

      <!-- 主题切换器 - dropdown -->
      <div class="dropdown dropdown-end">
        <div tabindex="0" role="button" class="btn btn-ghost btn-circle">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01" />
          </svg>
        </div>
        <ul tabindex="0" class="dropdown-content z-[1] menu p-2 shadow-lg bg-base-100 rounded-box w-40">
          <li><a onclick="setTheme('emerald')" class="flex items-center gap-2">
            <span class="w-3 h-3 rounded-full bg-emerald-500"></span>
            Emerald
          </a></li>
          <li><a onclick="setTheme('winter')" class="flex items-center gap-2">
            <span class="w-3 h-3 rounded-full bg-sky-500"></span>
            Winter
          </a></li>
          <li><a onclick="setTheme('light')" class="flex items-center gap-2">
            <span class="w-3 h-3 rounded-full bg-gray-200 border"></span>
            Light
          </a></li>
        </ul>
      </div>
    </div>

    <!-- 主内容区 - 单栏居中 -->
    <div class="max-w-5xl mx-auto px-4 py-12">

      <!-- Hero 区域 - 极简 -->
      <div class="text-center mb-12">
        <h1 class="text-5xl font-bold mb-4">Tool Index</h1>
        <p class="text-lg text-base-content/70">你的个人工具索引站 —— 快速搜索、一键直达</p>
      </div>

      <!-- 搜索框 - 药丸形 -->
      <div class="mb-12">
        <div class="flex justify-center">
          <div class="relative w-full max-w-2xl">
            <input
              id="search-input"
              type="text"
              placeholder="搜索工具..."
              class="input input-bordered input-lg rounded-full w-full pr-16 focus:input-primary transition-smooth"
            />
            <div class="absolute right-4 top-1/2 -translate-y-1/2 text-sm text-base-content/40 hidden md:block">
              ⌘K
            </div>
          </div>
        </div>
      </div>

      <!-- 分类过滤器 - badge -->
      <div class="mb-8">
        <div class="flex flex-wrap justify-center gap-2">
          <button
            data-category="all"
            class="category-btn badge badge-lg badge-primary cursor-pointer hover:badge-secondary transition-smooth"
          >
            全部 <span class="ml-1 text-base-content/70">({tools.length})</span>
          </button>
          {
            categoryList.map(([cat, count]) => (
              <button
                data-category={cat}
                class="category-btn badge badge-lg badge-ghost cursor-pointer hover:badge-secondary transition-smooth"
              >
                {cat} <span class="ml-1 text-base-content/70">({count})</span>
              </button>
            ))
          }
        </div>
      </div>

      <!-- 工具列表 - divide-y 分割线 -->
      <div id="tools-container">
        <!-- 空状态 -->
        <div id="empty-state" class="hidden text-center py-20">
          <div class="text-6xl mb-4">🔍</div>
          <h3 class="text-xl font-semibold mb-2">未找到匹配的工具</h3>
          <p class="text-base-content/60">尝试调整搜索词或选择其他分类</p>
        </div>

        <!-- 工具卡片列表 -->
        <div id="tools-list" class="divide-y divide-base-content/10">
          <!-- 由 JS 动态渲染 -->
        </div>

        <!-- 加载状态和哨兵元素 -->
        <div id="loading-container" class="py-8">
          <!-- 哨兵元素 - 始终可见,让 Observer 监听 -->
          <div id="loading-sentinel" class="min-h-[60px] flex items-center justify-center">
            <!-- 加载 spinner - 可以隐藏 -->
            <span id="loading-spinner" class="loading loading-spinner loading-primary"></span>
            <!-- 已加载全部消息 -->
            <div id="end-message" class="hidden text-center text-base-content/50 text-sm">
              —— 已加载全部 <span id="total-count">0</span> 条数据 ——
            </div>
          </div>
        </div>
      </div>

      <!-- Footer -->
      <footer class="mt-20 text-center text-sm text-base-content/50">
        <p>Built with <a href="https://astro.build" target="_blank" class="link link-hover">Astro</a> · DaisyUI · TailwindCSS</p>
      </footer>

    </div>

  </div>

  <!-- 服务端数据传递到客户端 -->
  <script define:vars={{ tools }}>
    window.__TOOLS__ = tools;
  </script>

  <!-- 客户端脚本 -->
  <script>
    // 从全局变量获取工具数据
    const toolsData = window.__TOOLS__ || [];
    console.log('📦 总数据量:', toolsData.length);

    // DOM 元素
    const searchInput = document.getElementById('search-input');
    const toolsListEl = document.getElementById('tools-list');
    const emptyStateEl = document.getElementById('empty-state');
    const loadingSentinel = document.getElementById('loading-sentinel');
    const loadingSpinner = document.getElementById('loading-spinner');
    const endMessage = document.getElementById('end-message');
    const totalCountEl = document.getElementById('total-count');
    const categoryButtons = document.querySelectorAll('.category-btn');

    // 配置
    const ITEMS_PER_PAGE = 15;

    // 简化的状态
    let allTools = [];           // 全部数据
    let filteredTools = [];      // 筛选后的数据
    let displayedCount = 0;      // 已显示数量
    let isLoading = false;       // 是否加载中
    let observer = null;         // Observer 实例

    // 搜索和筛选状态
    let currentCategory = 'all';
    let searchQuery = '';
    let fuse = null;

    // 初始化主题
    function initTheme() {
      const savedTheme = localStorage.getItem('theme') || 'emerald';
      document.documentElement.setAttribute('data-theme', savedTheme);
    }

    window.setTheme = function(theme) {
      document.documentElement.setAttribute('data-theme', theme);
      localStorage.setItem('theme', theme);
    };

    // 初始化 Fuse.js
    async function initFuse() {
      const { default: Fuse } = await import('fuse.js');

      fuse = new Fuse(toolsData, {
        keys: [
          { name: 'name', weight: 2 },
          { name: 'description', weight: 1.5 },
          { name: 'category', weight: 1 },
          { name: 'tags', weight: 1.2 }
        ],
        threshold: 0.4,
        includeScore: false,
        shouldSort: true,
        minMatchCharLength: 1,
        ignoreLocation: true
      });

      // 初始化数据
      allTools = toolsData;
      filteredTools = filterTools();
      console.log('🔍 筛选后数据量:', filteredTools.length);

      // 初始加载
      initObserver();
      loadNextPage();
    }

    // 过滤工具
    function filterTools() {
      let filtered = allTools;

      if (currentCategory !== 'all') {
        filtered = filtered.filter(tool => tool.category === currentCategory);
      }

      if (searchQuery.trim() && fuse) {
        const results = fuse.search(searchQuery);
        filtered = filtered.filter(tool =>
          results.some(r => r.item === tool)
        );
      }

      return filtered;
    }

    // 初始化 Observer
    function initObserver() {
      // 先断开旧的
      if (observer) {
        observer.disconnect();
      }

      // 创建新的 Observer
      observer = new IntersectionObserver(
        (entries) => {
          console.log('👀 Observer 触发', entries[0].isIntersecting);

          if (entries[0].isIntersecting && !isLoading) {
            console.log('✅ 开始加载下一页');
            loadNextPage();
          }
        },
        {
          rootMargin: '100px',
          threshold: 0.1
        }
      );

      // 确保元素存在再观察
      if (loadingSentinel) {
        observer.observe(loadingSentinel);
        console.log('🎯 Observer 已启动');
      }
    }

    // 加载下一页
    function loadNextPage() {
      // 防止重复加载
      if (isLoading) {
        console.log('⏸️  正在加载中,跳过');
        return;
      }

      // 检查是否已加载全部
      if (displayedCount >= filteredTools.length) {
        console.log('🏁 已加载全部数据');
        showEndState();
        return;
      }

      console.log(`📄 加载第 ${Math.floor(displayedCount / ITEMS_PER_PAGE) + 1} 页`);
      isLoading = true;

      // 显示加载 spinner
      loadingSpinner.classList.remove('hidden');
      endMessage.classList.add('hidden');

      // 计算要加载的数据
      const start = displayedCount;
      const end = Math.min(start + ITEMS_PER_PAGE, filteredTools.length);
      const newItems = filteredTools.slice(start, end);

      console.log(`📦 加载 ${newItems.length} 条数据 (第 ${start}-${end} 条)`);

      // 渲染数据
      renderTools(newItems, displayedCount === 0);

      // 更新计数
      displayedCount = end;
      isLoading = false;

      // 检查是否还有更多
      if (displayedCount >= filteredTools.length) {
        console.log('✅ 已加载全部');
        showEndState();
        if (observer) observer.disconnect();
      } else {
        console.log(`⏳ 还有 ${filteredTools.length - displayedCount} 条数据`);
        // 隐藏 spinner,但保持 loadingSentinel 可见
        loadingSpinner.classList.add('hidden');
      }
    }

    // 显示已加载全部状态
    function showEndState() {
      loadingSpinner.classList.add('hidden');
      endMessage.classList.remove('hidden');
      totalCountEl.textContent = displayedCount;
    }

    // 重置加载状态
    function resetLoading() {
      displayedCount = 0;
      isLoading = false;

      toolsListEl.innerHTML = '';
      loadingSpinner.classList.remove('hidden');
      endMessage.classList.add('hidden');

      // 重新过滤
      filteredTools = filterTools();
      console.log('🔄 重置加载,筛选后:', filteredTools.length, '条');

      // 重新初始化 Observer
      initObserver();

      // 立即加载第一页
      loadNextPage();
    }

    // 渲染工具卡片
    function renderTools(tools, isFirstPage) {
      if (tools.length === 0) {
        toolsListEl.classList.add('hidden');
        emptyStateEl.classList.remove('hidden');
        return;
      }

      toolsListEl.classList.remove('hidden');
      emptyStateEl.classList.add('hidden');

      const fragment = document.createDocumentFragment();

      tools.forEach((tool, index) => {
        const div = document.createElement('div');
        div.className = 'py-4 hover:shadow-md transition-smooth cursor-pointer';

        if (isFirstPage) {
          div.style.opacity = '0';
          div.style.transform = 'translateY(8px)';
          div.style.transition = `opacity 0.3s ease-out ${index * 0.05}s, transform 0.3s ease-out ${index * 0.05}s`;
        }

        div.innerHTML = `
          <div class="flex flex-col sm:flex-row justify-between gap-4">
            <div class="flex-1 min-w-0">
              <h3 class="text-lg font-semibold mb-1 text-primary hover:underline">
                <a href="${tool.url}" target="_blank" rel="noopener noreferrer">${tool.name}</a>
              </h3>
              <p class="text-sm text-base-content/70 mb-2 line-clamp-2">
                ${tool.description}
              </p>
              <div class="flex flex-wrap gap-1">
                <span class="badge badge-ghost badge-sm">${tool.category || '未分类'}</span>
                ${(tool.tags || []).map(tag =>
                  `<span class="badge badge-outline badge-sm">${tag}</span>`
                ).join('')}
              </div>
            </div>
            <div class="flex items-center sm:items-start">
              <a
                href="${tool.url}"
                target="_blank"
                rel="noopener noreferrer"
                class="btn btn-primary btn-sm rounded-full"
                aria-label="打开 ${tool.name}"
              >
                <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                </svg>
              </a>
            </div>
          </div>
        `;

        fragment.appendChild(div);
      });

      toolsListEl.appendChild(fragment);

      if (isFirstPage) {
        requestAnimationFrame(() => {
          const cards = toolsListEl.querySelectorAll('div.py-4');
          cards.forEach(card => {
            card.style.opacity = '1';
            card.style.transform = 'translateY(0)';
          });
        });
      }
    }

    // 更新分类按钮状态
    function updateCategoryButtons() {
      categoryButtons.forEach(btn => {
        const category = btn.getAttribute('data-category');
        if (category === currentCategory) {
          btn.classList.remove('badge-ghost');
          btn.classList.add('badge-primary');
        } else {
          btn.classList.add('badge-ghost');
          btn.classList.remove('badge-primary');
        }
      });
    }

    // 防抖函数
    function debounce(func, wait) {
      let timeout;
      return function executedFunction(...args) {
        const later = () => {
          clearTimeout(timeout);
          func(...args);
        };
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
      };
    }

    // 搜索输入处理
    searchInput.addEventListener('input', debounce((e) => {
      console.log('🔎 搜索:', e.target.value);
      searchQuery = e.target.value;
      resetLoading();
    }, 300));

    // 分类按钮点击处理
    categoryButtons.forEach(btn => {
      btn.addEventListener('click', () => {
        console.log('🏷️  分类:', btn.getAttribute('data-category'));
        currentCategory = btn.getAttribute('data-category') || 'all';
        updateCategoryButtons();
        resetLoading();
      });
    });

    // 快捷键支持
    document.addEventListener('keydown', (e) => {
      if ((e.metaKey || e.ctrlKey) && e.key === 'k') {
        e.preventDefault();
        searchInput.focus();
        searchInput.select();
      }
      if (e.key === 'Escape' && document.activeElement === searchInput) {
        searchInput.value = '';
        searchQuery = '';
        resetLoading();
        searchInput.blur();
      }
    });

    // 页面卸载时清理
    window.addEventListener('beforeunload', () => {
      if (observer) {
        observer.disconnect();
      }
    });

    // 添加样式
    const style = document.createElement('style');
    style.textContent = `
      #tools-list {
        scroll-margin-top: 80px;
      }
    `;
    document.head.appendChild(style);

    // 初始化
    console.log('🚀 开始初始化...');
    initTheme();
    initFuse();
  </script>
</Layout>
