---
interface Props {
  title?: string;
  description?: string;
}

const {
  title = '工具快速索引',
  description = '精选工具 · 搜索即达 · 点击即达'
} = Astro.props;
---

<!-- 标题 - Linear 风格：精致简洁 -->
  <h1 class="text-5xl md:text-6xl font-bold tracking-tight bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent">
    Batool
  </h1>
  <p class="mt-4 text-2xl text-base-content/90 max-w-2xl mx-auto font-medium leading-snug">
    直达，即专注。
  </p>
  <p class="mt-4 text-lg text-base-content/70 max-w-xl mx-auto">
    一个为行动者设计的工具入口 —— 无噪音，无干扰，<br class="hidden sm:block" />
    只为你与目标之间，留一条最短路径。
  </p>